//
//  Airport.swift
//  FlightMate
//
//  Domain model describing an airport (identifiers, runways, frequencies, etc.).
//

import Foundation

/// Represents an airport relevant to the current flight (departure, destination,
/// or nearby diversion). Fields to be defined.
struct Airport {
    // TODO: Define airport fields (ICAO, name, runways, frequencies, etc.)
}
